<h1><%= @chat_room.display_name_for(current_user) %></h1>

<%= turbo_stream_from @chat_room %>

<div id = 'messages' >
  <%= render @messages %>
</div>

<div id='message_form' >
  <%= render 'messages/form', chat_room: @chat_room, message: @message %>
</div>

<%= paginate @messages %>